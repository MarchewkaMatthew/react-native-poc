import 'react-native-gesture-handler';
import { StatusBar } from 'expo-status-bar';
import { Box, Container, Inner, Text } from './App.styles';

export default function App() {
  return (
    <Container>
      <Inner>
        <Text>JEBAĆ PIS</Text>
        <Box />
        <StatusBar style="auto" />
      </Inner>
    </Container>
  );
}
